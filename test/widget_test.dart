import 'package:flutter_test/flutter_test.dart';
import 'package:mi_vocho/main.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Test básico — se expandirá después
  });
}